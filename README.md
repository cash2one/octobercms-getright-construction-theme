# getRigh Construction

Theme to show your construction themes. Based on Bootstrap 3

## Activating the theme

`$ php artisan theme:use <name>`

Where `<name>` is whatever you specified in `theme:install`.
